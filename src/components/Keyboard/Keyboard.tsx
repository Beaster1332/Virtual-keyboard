import React from 'react';
import classes from './Keyboard.module.css';

const Keyboard = (props: any) => {

    const { keysRu, keysEng } = props;

    return <div className={classes.keyboardContainer}>
        gavno
    </div>
}

export default Keyboard;